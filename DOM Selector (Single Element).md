#dom #dom-selectors-single 
```js
const element = document.getElementById('elementId');

const element = document.querySelector('.className');
const element = document.querySelector('.parent .child')
```
